# 📁 Assets Folder

## 🎯 الملفات المطلوبة

### 1. **speech_model.tflite** ⚠️ (مطلوب)
```
النوع: نموذج TensorFlow Lite
الوصف: نموذج تحويل الصوت إلى نص
الحجم المتوقع: 5-50 MB
المصدر: قم بإضافته بنفسك
```

**كيف تحصل عليه:**
1. استخدم نموذج جاهز من:
   - Hugging Face
   - TensorFlow Hub
   - مصادر أخرى للنماذج العربية

2. أو درّب نموذجك الخاص:
   - استخدم مجموعة بيانات صوتية عربية
   - حوّله إلى TFLite
   - ضعه هنا

**المواصفات المتوقعة:**
```
Input:
- Type: Float32
- Shape: [1, 16000] (1 second at 16kHz)
- Range: -1.0 to 1.0 (normalized audio)

Output:
- Type: Float32
- Shape: [1, vocabulary_size]
- Values: Probabilities for each word/character
```

---

### 2. **vocabulary.txt** ✅ (موجود - اختياري)
```
النوع: ملف نصي
الوصف: قائمة الكلمات/الأحرف المتوقعة
الحجم: صغير (~1 KB)
الحالة: موجود كمثال
```

**المحتوى:**
```
كل سطر = كلمة أو حرف
الترتيب مهم (يجب أن يتطابق مع مخرجات النموذج)
```

**مثال:**
```
افتح
أقفل
اتصل
شغل
اطفي
...
```

---

## 📝 ملاحظات مهمة

### ⚠️ النموذج غير موجود حالياً
التطبيق جاهز للعمل، لكن تحتاج إضافة ملف النموذج:
```
speech_model.tflite ← أضف هنا
```

### ✅ ملف المفردات موجود
تم إضافة ملف مثال يحتوي على:
- أوامر التطبيق الشائعة
- أسماء التطبيقات
- كلمات عربية وإنجليزية

يمكنك تعديله حسب نموذجك!

---

## 🚀 خطوات الإعداد

### الخطوة 1: إضافة النموذج
```bash
1. احصل على ملف speech_model.tflite
2. ضعه في هذا المجلد:
   app/src/main/assets/speech_model.tflite
```

### الخطوة 2: تعديل المفردات (اختياري)
```bash
1. افتح vocabulary.txt
2. عدّل القائمة لتتطابق مع نموذجك
3. احفظ
```

### الخطوة 3: تعديل الكود (إذا لزم)
```bash
إذا كان نموذجك له مواصفات مختلفة:
1. افتح SpeechRecognizer.kt
2. عدّل:
   - inputSize (السطر 24)
   - prepareInputBuffer() (السطر 188)
   - processOutput() (السطر 213)
```

---

## 🔧 نماذج مقترحة

### للعربية:
```
1. Wav2Vec 2.0 Arabic (Hugging Face)
2. ArabicSpeech models
3. نماذج مخصصة من Google Speech API
```

### للإنجليزية (للاختبار):
```
1. DeepSpeech
2. Vosk models
3. TensorFlow Speech Commands
```

---

## 📊 حجم الملفات المتوقع

```
speech_model.tflite:   5-50 MB
vocabulary.txt:        1-10 KB
```

**إجمالي:** ~50 MB كحد أقصى

---

## ✅ الخلاصة

```
✅ vocabulary.txt موجود
⚠️ speech_model.tflite مطلوب (أضفه أنت)
📝 التطبيق جاهز للعمل بمجرد إضافة النموذج
```

---

## 🆘 المساعدة

إذا كنت لا تملك نموذج حالياً:
1. التطبيق سيعمل لكن التعرف على الصوت لن يعمل
2. يمكنك اختبار باقي المميزات
3. عند الحصول على النموذج، ضعه هنا وسيعمل فوراً

للحصول على نماذج:
- Hugging Face: https://huggingface.co/models?language=ar&pipeline_tag=automatic-speech-recognition
- TensorFlow Hub: https://tfhub.dev/
- Google AI: https://ai.google.dev/
